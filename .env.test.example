# Test User Credentials
# Create a test user in Supabase Dashboard > Authentication > Users
# Copy this file to .env.test and fill in the values

# Regular test user (non-admin, email confirmed)
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=your-test-password

# Admin test user (must be in adminConfig.ts admin list)
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=your-admin-password

# Optional: User with unconfirmed email (for testing email confirmation warning)
TEST_UNCONFIRMED_EMAIL=unconfirmed@example.com
TEST_UNCONFIRMED_PASSWORD=your-password
